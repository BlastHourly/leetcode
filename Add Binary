/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
var addBinary = function(a, b) {
    
    let convertedValueA = BigInt(`0b${a}`);
    let convertedValueB = BigInt(`0b${b}`);   
    
    return (convertedValueA+convertedValueB).toString(2);
    
};
